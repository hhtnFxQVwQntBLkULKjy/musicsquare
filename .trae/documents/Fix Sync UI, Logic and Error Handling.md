# 修复同步 UI、同步接口及错误提示优化

## 1. 前端：优化同步弹窗 UI 与提示
- **文件**: `js/app.js`
- **操作**: 
    - 修改 `showIdInput` 函数，为 QQ音乐、咪咕音乐等平台添加详细的 ID 获取提示（“分享歌单到微信/QQ -> 浏览器打开 -> URL中查找 ID”）。
    - 优化“开始同步”按钮的 CSS 样式，修复显示问题。

## 2. 前端：优化“添加到歌单”的错误处理
- **文件**: `js/service.js`
- **操作**: 修改 `addSongToPlaylist` 方法，使其在请求失败时**抛出后端返回的具体错误信息**（如“Song already exists”），而不是简单返回 `false`。
- **文件**: `js/ui.js`
- **操作**: 更新 `showPlaylistSelect` 逻辑，捕获并展示具体的错误原因（例如提示“添加失败：歌曲已存在”），以便您准确判断问题。

## 3. 后端：更换同步 API 源
- **文件**: `backend/worker.js`
- **操作**: 
    - 目前的 Meting API 源似乎不支持用户歌单同步 (`type=user`)。我将把 API 源更换为 `https://api.wuenci.com/meting/api/` (或其他备选源)，尝试修复无法获取歌单的问题。
    - 检查 `Add Song` 的后端查重逻辑，确保 ID 匹配规则正确。

## 4. 确认：歌单刷新逻辑
- **说明**: 我已核实代码，当前**每次点击侧边栏歌单时，确实会强制从后端拉取最新数据**。这解释了为什么您点击歌单后能看到新添加的歌曲。此逻辑是正确的，无需修改。
